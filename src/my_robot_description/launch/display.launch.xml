<?xml version="1.0"?>
<launch>

    <!-- Declare launch arguments -->
    <arg name="use_sim_time" default="True" description="Use simulation (Gazebo) clock if true" />
    <arg name="robot_model" default="rubot/rubot_mecanum.urdf" description="Robot model name" />

    <!-- Define paths based on the robot model -->
    <let name="urdf_path" 
         value="$(find-pkg-share my_robot_description)/urdf/$(var robot_model)" />
    <let name="rviz_config_path"
         value="$(find-pkg-share my_robot_description)/rviz/urdf_config.rviz" />

    <!-- Publish robot state -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description"
               value="$(command 'xacro $(var urdf_path)')" />
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

    <!-- Joint state publisher -->
    <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui">
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

    <!-- RViz -->
    <node pkg="rviz2" exec="rviz2" output="screen" 
          args="-d $(var rviz_config_path)">
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

</launch>
